# Dummy Lora Receiver
Tool to generate dummy LoRa parsed data output. 

> It is just _lora-cli_ but without Lora!

# Run
```shell
cargo run
```

# Build
```shell
cargo build
# or
cargo build --release
```